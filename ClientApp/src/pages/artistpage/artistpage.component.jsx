import React from 'react';

//import artistpage from 'artistpage.styles.scss';

import ScrollableTabsButtonForce from '../../components/forced-scrolled-buttons/forced-scrolled-buttons.js';

const ArtistPage = () => (
    <div>
        <ScrollableTabsButtonForce />
    </div>  
);

export default ArtistPage;